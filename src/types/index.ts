// src/types/index.ts

// This file is intended to hold shared TypeScript types for this microservice.
// As the service evolves, you can define interfaces, types, enums, etc., here.

export interface ExampleType {
  id: string;
  name: string;
  value: number;
}

// Add other types below as needed 